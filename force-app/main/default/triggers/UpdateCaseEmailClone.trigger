trigger UpdateCaseEmailClone on EmailMessage (after insert){
//    UpdateCaseEmailHandler.updateRMATask(Trigger.new);
//    if(!system.isBatch()){ 
//      UpdateCaseEmailHandler.fetchAttachment(Trigger.newMap.keySet());
//    }
}